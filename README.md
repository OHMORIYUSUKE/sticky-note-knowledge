# sticky-note-knowledge

```bash
# start server
cd server
yarn  # install libraries
yarn prisma:generate
yarn prisma:migrate
yarn prisma:seed
yarn dev

# start client
cd client
yarn  # install libraries
yarn dev
```
